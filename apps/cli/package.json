{
  "name": "@repo/cli",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build:darwin-arm64",
    "build:darwin-arm64": "bun build src/index.ts --compile --minify --bytecode --format=esm --target=bun-darwin-arm64 --outfile dist/psst",
    "check:types": "bun run --bun tsc"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "catalog:"
  },
  "imports": {
    "#*": "./src/*"
  },
  "dependencies": {
    "@opentui/core": "^0.1.96",
    "@stricli/core": "^1.2.6",
    "picocolors": "^1.1.1"
  }
}
